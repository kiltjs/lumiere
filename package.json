{
  "name": "lumiere",
  "version": "0.0.5",
  "description": "Animation wrapper",
  "main": "lumiere.js",
  "scripts": {
    "test": "make test",
    "build": "rollup src/lumiere.js --format cjs --output lumiere.js; rollup src/lumiere.js --format umd --output lumiere.umd.js -n lumiere; uglifyjs lumiere.umd.js -o lumiere.min.js -c -m",
    "build-duration": "rollup src/detect-duration.js --format cjs --output detect-duration.js; rollup src/detect-duration.js --format umd --output detect-duration.umd.js -n detectDuration; uglifyjs detect-duration.umd.js -o detect-duration.min.js -c -m"
  },
  "keywords": [
    "animation",
    "frames",
    "progress"
  ],
  "author": "Jesús Germade <jesus@germade.es>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kiltjs/lumiere.git"
  },
  "bugs": {
    "url": "https://github.com/kiltjs/lumiere/issues"
  },
  "homepage": "https://github.com/kiltjs/lumiere#readme",
  "devDependencies": {
    "rollup": "^0.56.3",
    "uglify-js": "^3.3.12"
  }
}
