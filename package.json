{
  "name": "lumiere",
  "version": "0.0.7",
  "description": "Animation wrapper",
  "main": "lumiere.js",
  "scripts": {
    "test": "make test",
    "prebuild": "mkdir -p dist",
    "build": "rollup src/lumiere.js --format cjs --output dist/lumiere.js; rollup src/lumiere.js --format umd --output dist/lumiere.umd.js -n lumiere; uglifyjs dist/lumiere.umd.js -o dist/lumiere.min.js -c -m",
    "build-duration": "rollup src/detect-duration.js --format cjs --output dist/detect-duration.js; rollup src/detect-duration.js --format umd --output dist/detect-duration.umd.js -n detectDuration; uglifyjs dist/detect-duration.umd.js -o dist/detect-duration.min.js -c -m",
    "postbuild": "npm run build-duration; cp package.json dist/package.json; cp README.md dist/README.md"
  },
  "keywords": [
    "animation",
    "frames",
    "progress"
  ],
  "author": "Jesús Germade <jesus@germade.es>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kiltjs/lumiere.git"
  },
  "bugs": {
    "url": "https://github.com/kiltjs/lumiere/issues"
  },
  "homepage": "https://github.com/kiltjs/lumiere#readme",
  "devDependencies": {
    "rollup": "^0.56.3",
    "uglify-js": "^3.3.12"
  }
}
